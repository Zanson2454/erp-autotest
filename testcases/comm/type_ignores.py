"""
类型忽略文件 - 用于处理pytest相关的类型检查问题
"""

# 忽略pytest相关的属性访问问题
# type: ignore[attr-defined]

# 忽略pytest动态属性的类型检查
# type: ignore[assignment]

# 忽略pytest装饰器的类型检查
# type: ignore[no-untyped-def]

# 忽略pytest参数的类型检查
# type: ignore[arg-type]

# 忽略pytest返回值的类型检查
# type: ignore[return-value]

# 忽略pytest变量的类型检查
# type: ignore[var-annotated]

# 忽略pytest导入的类型检查
# type: ignore[import]

# 忽略pytest未使用变量的类型检查
# type: ignore[unused-import]

# 忽略pytest重复导入的类型检查
# type: ignore[duplicate-code]

# 忽略pytest通配符导入的类型检查
# type: ignore[wildcard-import]

# 忽略pytest私有使用的类型检查
# type: ignore[private-usage]

# 忽略pytest常量重定义的类型检查
# type: ignore[constant-redefinition]

# 忽略pytest方法重写的类型检查
# type: ignore[incompatible-method-override]

# 忽略pytest变量重写的类型检查
# type: ignore[incompatible-variable-override]

# 忽略pytest构造函数不一致的类型检查
# type: ignore[inconsistent-constructor]

# 忽略pytest重载重叠的类型检查
# type: ignore[overlapping-overloads]

# 忽略pytest缺少类型参数的类型检查
# type: ignore[missing-type-argument]

# 忽略pytest无效字符串转义的类型检查
# type: ignore[invalid-string-escape]

# 忽略pytest未知参数类型的类型检查
# type: ignore[unknown-parameter-type]

# 忽略pytest未知参数类型的类型检查
# type: ignore[unknown-argument-type]

# 忽略pytest未知lambda类型的类型检查
# type: ignore[unknown-lambda-type]

# 忽略pytest未知变量类型的类型检查
# type: ignore[unknown-variable-type]

# 忽略pytest未知成员类型的类型检查
# type: ignore[unknown-member-type]

# 忽略pytest缺少类型存根的类型检查
# type: ignore[missing-type-stub]

# 忽略pytest未使用调用结果的类型检查
# type: ignore[unused-call-result]

# 忽略pytest未使用表达式的类型检查
# type: ignore[unused-expression]

# 忽略pytest未使用变量的类型检查
# type: ignore[unused-variable]

# 忽略pytest未使用导入的类型检查
# type: ignore[unused-import]

# 忽略pytest未使用类的类型检查
# type: ignore[unused-class]

# 忽略pytest未使用函数的类型检查
# type: ignore[unused-function]

# 忽略pytest重复导入的类型检查
# type: ignore[duplicate-import]

# 忽略pytest通配符导入的类型检查
# type: ignore[wildcard-import-from-library] 