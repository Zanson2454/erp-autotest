---
description: 
globs: 
alwaysApply: false
---
# 业务与用例规范

## 测试用例编写规范
[always]
1. 所有测试用例都需要加上基础断言  assert_response_success方法,来自 utils/assert_util.py 中的AssertHelper类
2. 所有测试文件都需要添加项目根目录到 Python 路径,例如：
   '''
   project_root = Path(__file__).resolve().parent.parent.parent
   sys.path.insert(0, str(project_root))
   '''
3. 所有测试文件都依赖 testcases/comm/base_test.py 中的BaseTest类,且都必须有 setup_class
   '''
     @classmethod
       def setup_class(cls):
           """测试类初始化"""
           super().setup_class()
   '''
4. 基于 curl 生成测试用例时,所有入参都去除teamId 和 appId
5. 所有用例都要加上异常捕获

# 测试用例命名规范
[always]
1. 测试文件命名：
   - 格式：test_序号_模块名_功能.py
   - 示例：test_01_create.py, test_02_orderlist_query.py
   - 序号：00-配置检查, 01-创建, 02-查询, 03-操作, 04-删除
2. 测试类命名：
   - 格式：Test + 模块名 + 功能 or TEST_功能
   - 示例：TestSalesOrderCreate, TestOrderList, TestSalesOrderOperator
   - 类名要能清晰表达测试的功能范围
3. 测试方法命名：
   - 格式：test_序号_操作_对象_条件 or test_功能_结果
   - 示例：
     - test_01_query_orders（基础查询）
     - test_02_query_orders_by_so_code（按订单编号查询）
     - test_03_query_orders_by_status（按状态查询）
     - test_04_query_orders_by_so_type（按类型查询）
     - test_05_query_orders_by_customer（按客户查询）
4. 测试数据命名：
   - 类属性：使用业务含义命名
     - so_type_id（订单类型ID）
     - cust_id（客户ID）
     - sls_org_id（销售组织ID）
   - 方法内变量：使用业务含义命名
     - order（订单对象）
     - result（查询结果）
     - response（接口响应）
5. 测试用例组织：
   - 按业务模块组织测试文件
   - 按功能点组织测试类
   - 按测试场景组织测试方法
   - 使用 pytest.mark.order 控制执行顺序
   - 使用 pytest.mark.parametrize 处理参数化测试
6. 测试用例依赖：
   - 使用 pytest.mark.dependency 标记依赖关系
   - 使用 setup_class 处理类级别的依赖
   - 使用 setup_method 处理方法级别的依赖
   - 使用 teardown_class 处理清理工作
7. 测试用例文档：
   - 类文档：说明测试类的功能范围
   - 方法文档：说明测试方法的测试步骤
   - 使用 allure 装饰器添加测试报告信息
   - 使用 logger 记录测试执行过程
8. 测试用例断言：
   - 使用 assert_util 中的断言方法
   - 每个断言都要有明确的错误信息
   - 断言要验证关键业务逻辑
   - 断言要验证数据完整性
9. 测试用例日志：
   - 使用 logger 记录关键操作
   - 记录测试用例的执行步骤
   - 记录测试数据的准备和清理
   - 记录断言的结果
10. 测试用例异常处理：
    - 使用 safe_api_call 装饰器处理异常
    - 捕获并记录所有可能的异常
    - 提供清晰的错误信息
    - 确保测试环境清理

# 测试用例组织方式
[always]
1. 按业务模块组织测试用例
2. 相关测试用例放在同一个测试类中
3. 测试用例之间保持独立性

# 测试数据管理规范
[recommended]
1. 测试数据准备：
   - 使用 fixture 管理测试数据
   - 测试数据与测试用例分离
   - 使用 yaml 文件存储测试数据
   - 测试数据命名规范统一
2. 测试数据验证：
   - 验证测试数据的完整性
   - 验证测试数据的正确性
   - 验证测试数据的清理结果

# 测试用例依赖关系处理
[recommended]
1. 使用 pytest.mark.dependency 标记依赖关系
2. 使用 pytest.mark.order 控制执行顺序
3. 使用 fixture 的 scope 控制数据共享范围
4. 使用 setup_class 和 teardown_class 处理类级别的依赖
