---
description: 
globs: 
alwaysApply: false
---
# 依赖管理与环境配置规范
[always]
1. 所有依赖安装指令必须使用 `uv pip install` 格式，禁止直接使用 pip install。
2. 优先推荐使用 `requirements-dev.txt` 管理测试依赖，所有测试相关依赖（如 pytest、allure-pytest、mock 等）必须写入 requirements-dev.txt，生产依赖单独维护 requirements.txt（如有）。
3. 生成 Allure 报告时需包含环境变量配置（如 JAVA_HOME）。
4. 依赖升级需在CI中验证兼容性。
5. 推荐在 README.md 和 requirements-dev.txt 头部注释依赖管理方式。
