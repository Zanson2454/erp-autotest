[tool.ruff]
target-version = "py39"
line-length = 120
exclude = [
  ".git",
  ".venv",
  ".pytest_cache",
  "reports",
  "logs",
  "outputs",
  "static",
  "erp_data_factory/legacy",
]

[tool.ruff.lint]
select = [
  "E",
  "F",
  "I",
]
ignore = [
  "E501",
]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
line-ending = "lf"

[tool.pytest.ini_options]
addopts = "--tb=short"
